apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-node
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demo-node
  template:
    metadata:
      labels:
        app: demo-node
    spec:
      containers:
      - name: demo-node
        image: 9680264173/argocdnodedemo:v3
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
